# Copie para .env e preencha com seus valores.
# Nunca commite o arquivo .env (ele está no .gitignore).

# Supabase PostgreSQL (use a connection string do painel Supabase)
# Para serverless (Vercel), prefira a URI com Connection pooling (porta 6543).
# Senha com @ ou , use URL-encode: @ -> %40, , -> %2C
DATABASE_URL=postgresql://postgres:SENHA@db.SEUPROJETO.supabase.co:5432/postgres

# Produção (Vercel): defina DATABASE_URL e SECRET_KEY no dashboard do projeto.
# Gere SECRET_KEY com: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# SECRET_KEY=sua-chave-secreta-django
# DEBUG=0
# ALLOWED_HOSTS=.vercel.app,.now.sh
# Produção (Vercel): evita 500 em login/formulários. Use a URL do deploy, ex:
# CSRF_TRUSTED_ORIGINS=https://seu-projeto.vercel.app
